import 'package:flutter/material.dart';

class AddQuotationPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: new Text('Add quotation'),
      ),
    );
  }
}